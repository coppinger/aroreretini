---
import { slugOptions } from "@lib/slug";
const { tag } = Astro.props;
---

<a
    href={`/tags/${slugOptions(tag)}`}
    class="flex p-2 border-2 border-slate-700 text-xs text-slate-300 w-fit uppercase tracking-widest font-bold"
    >{tag.split("-").join(" ")}</a
>
